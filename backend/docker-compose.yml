version: "3.9"

services:
  backend:
    build: .
    container_name: skilldeck_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PORT=8000
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
